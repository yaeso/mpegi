[tool.poetry]
name = "mpegi"
version = "0.1.0"
description = ""
authors = ["yaeso"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pytest = "^8.2.1"
numpy = "^1.26.4"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
